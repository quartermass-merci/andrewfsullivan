# dependencies & build output
node_modules/
site/node_modules/
site/dist/
dist/

# TypeScript incremental build cache
*.tsbuildinfo

# local migration archive — huge source GIFs + scraped originals, not needed
# in the repo (the web copies the site uses live in site/public/)
Assets from the Current Website/

# favicon source images — the generated favicons live in site/public/
Favicon/

# OS / editor cruft
.DS_Store
**/.DS_Store
*.log

# env
.env
site/.env
site/.env.*
